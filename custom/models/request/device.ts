export interface IGroupIndexC {
    areaId: string;
    name: string;
}

export interface IGroupIndexU {
    objectId: string;
}

export interface IGroupAll {
    siteId?: string;
    areaId?: string;
}