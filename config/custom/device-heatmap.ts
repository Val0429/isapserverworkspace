export interface Config {
    productId: string;
    gridUnit: number;
    valueZoom: number;
}

let config: Config = {
    productId: '',
    gridUnit: 10,
    valueZoom: 6,
};
export default config;
